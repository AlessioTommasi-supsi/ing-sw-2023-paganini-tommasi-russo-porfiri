package org.example.Model;

public enum ChoiceMyShelfie {
    DRAW_FROM_BOARD,
    TERMINATE_TURNS,
    JOIN_GAME,
    SHOW_MY_SHELVES, //per guardare la mia libreria non richiamerò server ma verrà vista quella che ho in locale!!
    //che ogni volta che immetto qualcosa con IMMMETTI_IN_LIBRERIA verrà aggiornata!

    SHOW_BOARD,
    SHOW_COMMON_CARDS,
    SHOW_PERSONAL_CARDS,


    //.DEBUG
    EXIT
}
